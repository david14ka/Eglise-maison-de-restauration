<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eglisemr">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EgliseMr.NoActionBar.NoActionBar">

        <activity
            android:name=".LoginSignUpActivity"
            android:label="@string/title_activity_login_signup"
            android:theme="@style/Theme.EgliseMr.NoActionBar.NoActionBar.NoActionBar" />

        <activity android:name=".ui.message.VideoViewActivity" />
        <activity android:name=".ui.message.VideoActivity" />
        <activity
            android:name=".ui.webveiw.WebActivity"
            android:label="@string/title_activity_web"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.EgliseMr.NoActionBar.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_home"
            android:theme="@style/Theme.EgliseMr.NoActionBar.NoActionBar" />
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.message.MessageDetailActivity" />
        <activity android:name=".ui.login.StartSignInActivity" />

        <meta-data
            android:name="android.webkit.WebView.EnableSafeBrowsing"
            android:value="false" />
    </application>

</manifest>